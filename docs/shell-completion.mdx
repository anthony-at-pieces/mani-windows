# Shell Completion

Shell completion is available for PowerShell.

```powershell
mani completion powershell | Out-String | Invoke-Expression
```

To load completions for every new PowerShell session:

```powershell
mani completion powershell > mani.ps1
```

Then source `mani.ps1` from your PowerShell profile.
